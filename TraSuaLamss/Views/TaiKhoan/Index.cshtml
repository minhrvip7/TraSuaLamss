@model IEnumerable<TraSuaLamss.Models.TaiKhoan>

@{
    ViewBag.Title = "Index";
}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Họ tên
        </th>
        <th>
            Tên đăng nhập
        </th>
        <th>
            Phân quyền
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HoTen)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Username)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhanQuyen)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Username.Trim() }) |
            @Html.ActionLink("Details", "Details", new { id=item.Username.Trim() }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Username.Trim() })
        </td>
    </tr>
}

</table>
